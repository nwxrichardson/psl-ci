name: Comment on PRs

on:
  pull_request:
    branches: [ "main" ]

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Corvus comment
        id: comment
        uses: epsilonlabs/sirius-pr-action@main
        
        
