name: Comment on PRs

on:
  pull_request:
    branches: [ "main" ]

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Corvus comment
        id: comment
        uses: nwxrichardson/ci-corvus@v0
        
        
